[**rune**](README.md)

***

[rune](README.md) / loadYamlSync

# Function: loadYamlSync()

```ts
function loadYamlSync<T>(path): T;
```

Defined in: assets/load.ts:31

Read and parse a YAML file synchronously.

## Type Parameters

### T

`T`

## Parameters

### path

`string`

Filesystem path to the YAML file.

## Returns

`T`

The parsed contents, typed as `T`.
