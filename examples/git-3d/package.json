{
  "name": "git-3d",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gource-style 3D visualizer that replays a git repository's history",
  "scripts": {
    "dev": "rune dev",
    "build": "rune build",
    "install": "bun run install.ts"
  },
  "dependencies": {
    "@ahokinson/rune": "workspace:*",
    "@opentui/solid": "*",
    "@opentui/core": "*",
    "solid-js": "^1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
